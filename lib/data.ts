/**
 * Este arquivo continha dados mockados e foi substituído pela integração com Supabase.
 * Os dados agora são buscados diretamente do banco de dados através das APIs.
 *
 * Para usar o sistema:
 * 1. Configure as variáveis de ambiente no arquivo .env.local (use .env.local.example como base)
 * 2. Execute o SQL em migrations/schema.sql no SQL Editor do Supabase
 * 3. As APIs em app/api/ agora buscam dados diretamente do Supabase
 *
 * Referências:
 * - Server Client: lib/supabase/server.ts
 * - Tipos: lib/supabase/types.ts
 * - Schema: migrations/schema.sql
 */
